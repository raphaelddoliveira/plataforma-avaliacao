name: autoavaliacao_app
description: Aplicativo de autoavaliação para colaboradores
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.5.0
  
  # UI
  flutter_rating_bar: ^4.0.1
  intl: ^0.19.0
  
  # Utilities
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
